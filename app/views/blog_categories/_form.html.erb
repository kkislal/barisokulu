<%= form_with(model: blog_category, local: true) do |form| %>
  <% if blog_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog_category.errors.count, "error") %> prohibited this blog_category from being saved:</h2>

      <ul>
      <% blog_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>
  <br>

  <div class="input-field">
    <%= form.file_field :img, id: :blog_category_img %>
  </div>
  <br>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
